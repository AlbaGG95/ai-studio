# Examples

Use this folder to store optional sample states or project snapshots for demos. Runtime files under `data/` are ignored in git; if you need to share a reference state, copy it here instead of persisting it from a running session.
